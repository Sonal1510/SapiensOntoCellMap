# SapiensOntoCellMap — runtime dependencies
# Install with:  uv pip install -r requirements.txt
# Or editable:   uv pip install -e .
#
# For a fully-pinned reproducible environment (CI / exact reproduction):
#   uv pip install -r requirements-dev.txt

pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
plotly>=6.0
matplotlib>=3.7
seaborn>=0.12
jinja2>=3.1
openpyxl>=3.1
requests>=2.28
networkx>=3.0
tqdm>=4.60
cellxgene_ontology_guide>=1.7
scikit-learn>=1.2
hra-api-client>=0.16
h5py>=3.8
rapidfuzz>=3.0
